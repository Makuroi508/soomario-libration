flask>=3.0
requests>=2.31
python-dotenv>=1.0
hyperliquid-python-sdk>=0.22.0
eth-account>=0.13.0
# Solana venues (Pacifica / Bulk)
base58>=2.1
solders>=0.21
# >=0.1.26: BULK mainnet (API v1.0.19). bulk_client passes the signature
# domain explicitly (BULK_NETWORK) and uses prepare_order/sign_prepared for
# agent-wallet signing; both need 0.1.26+. Linux cp313 wheels exist.
bulk-keychain>=0.1.26
pynacl>=1.5
