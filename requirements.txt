flask>=3.0
requests>=2.31
python-dotenv>=1.0
hyperliquid-python-sdk>=0.22.0
eth-account>=0.13.0
# Solana venues (Pacifica / Bulk)
base58>=2.1
solders>=0.21
# PINNED: 0.1.20+ made Signer(signature_domain) a required argument and
# breaks bulk_client (written and tested against 0.1.19). BULK mainnet has
# not started; when it does, upgrade deliberately and pass the correct
# signature_domain rather than letting the floor float onto an API break.
bulk-keychain==0.1.19
pynacl>=1.5
